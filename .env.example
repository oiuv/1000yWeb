# 数据库配置 - 云端千年-炎黄新章
# 复制此文件为 .env 并修改以下配置

# 数据库服务器配置
DB_SERVER=localhost          # 数据库服务器地址（本地用localhost，远程用IP地址）
DB_DATABASE=1000y            # 数据库名称
DB_USER=sa                   # 数据库用户名
DB_PASSWORD=your_sql_password_here  # 数据库密码（请替换为实际密码）
DB_ENCRYPT=false             # 是否加密连接（本地通常为false，远程服务器可能需要true）

# 服务器配置
PORT=8080                    # Web服务器端口（如被占用可改为3000、8081等）
NODE_ENV=development         # 环境模式（development/production）

# 配置说明：
# 1. 本地开发：DB_SERVER=localhost，DB_PASSWORD设置你的SQL Server密码
# 2. 线上部署：DB_SERVER=数据库服务器IP，DB_PASSWORD=线上数据库密码
# 3. 端口冲突：修改PORT为其他可用端口（如3001、8081等）
# 4. 数据库连接失败请检查：
#    - SQL Server服务是否启动
#    - 用户名密码是否正确
#    - 防火墙是否允许1433端口
#    - 数据库是否存在（数据库名：1000y）